# 项目说明（AI Workspace）

本文件在 **创建/校验 `codestatrFirst` 下五目录之后立即生成或更新**，请勿删除本段约定。

## 生成时间

2026-05-22（文档修订：移除乱码副本、对齐江苏中医在线规格）

## 仓库根目录

`D:\workerdome`

自动化脚本位于全局 **`%USERPROFILE%\.cursor\skills\aiworkspace-manager\toolkit\`**，与 **codestatrFirst/skills/**（AI Skill 规则与脚本存放区，由本工作区规范使用）不是同一概念。

## 工作区根路径

`D:\workerdome\codestatrFirst`

其下固定包含五类目录：

| 目录 | 用途 |
|------|------|
| agents/ | Agent 定义与说明 |
| skills/ | AI Skill 规则与可迁移脚本 |
| cache/ | 可迁移缓存（如 JSON） |
| specs/ | 需求模块：spec.md、design.md、tasks.md |
| bugfix/ | 问题与修复记录（Markdown） |

## 江苏中医在线 — 规格索引

| 文档 | 路径 |
|------|------|
| 子模块索引与并行指南 | [specs/tcm-online-platform/README.md](specs/tcm-online-platform/README.md) |
| 平台总览 spec | [specs/tcm-online-platform/spec.md](specs/tcm-online-platform/spec.md) |
| 技术设计 | [specs/tcm-online-platform/design.md](specs/tcm-online-platform/design.md) |
| 七人分工 | [specs/tcm-online-platform/team-assignment.md](specs/tcm-online-platform/team-assignment.md) |
| 并行计划 | [specs/tcm-online-platform/parallel-plan.md](specs/tcm-online-platform/parallel-plan.md) |
| UI 说明 | [specs/tcm-online-platform/ui-prototypes.md](specs/tcm-online-platform/ui-prototypes.md) |
| 团队缓存 JSON | [cache/tcm-online-platform.json](cache/tcm-online-platform.json) |
| 代码编写规范 V2.0 | [../江苏中医在线项目 - 代码编写规范文档.md](../江苏中医在线项目 - 代码编写规范文档.md) |

**18 个子模块**（`specs/00-platform-foundation` … `specs/17-user-center`，含 `16-official-web` 官网展示），各含 `spec.md` / `design.md` / `tasks.md`。

## 常用命令（在项目根执行）

```powershell
# 初始化工作区
powershell -NoProfile -ExecutionPolicy Bypass -File "$env:USERPROFILE\.cursor\skills\aiworkspace-manager\toolkit\init-workspace.ps1" -Root "D:\workerdome"

# 新建模块三件套
powershell -NoProfile -ExecutionPolicy Bypass -File "$env:USERPROFILE\.cursor\skills\aiworkspace-manager\toolkit\new-module-spec.ps1" -Root "D:\workerdome" -ModuleName "<模块名>"

# 单条需求
powershell -NoProfile -ExecutionPolicy Bypass -File "$env:USERPROFILE\.cursor\skills\aiworkspace-manager\toolkit\auto-requirement.ps1" -Root "D:\workerdome" -Title "<标题>" -Requirement "<描述>" -Priority P1

# 记 bugfix
powershell -NoProfile -ExecutionPolicy Bypass -File "$env:USERPROFILE\.cursor\skills\aiworkspace-manager\toolkit\new-bugfix.ps1" -Root "D:\workerdome" -Title "<标题>"
```

## 路径约定

- 需求与缓存路径均以 **`codestatrFirst`** 为根（例如：`codestatrFirst/specs/<module>/`）。
- 若你曾在仓库根下直接放置过 `specs/` 等旧数据，请自行迁入 `codestatrFirst/` 下对应目录。
- 项目说明文件名必须为 **`项目说明.md`**（UTF-8）。若出现 `椤圭洰璇存槑.md` 等乱码文件名，为编码错误产生的副本，应删除并只保留本文件。

## 项目资料（仓库根）

- `01 中医在线项目介绍.md`
- `管理端使用手册.md`
- `用户端使用手册.md`
- `jiangsu-tcm-online/` — 后端工程（Spring Boot）
